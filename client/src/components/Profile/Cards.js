import Card from "./Card";

const Cards = (cards) => {
  return (
    <>
     {
        // cards.map(card => {
        //     return <Card props={...card}/>
        // })
        
     }
    </>
  );
};

export default Cards;
